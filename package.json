{
  "name": "webgpu-texture-streaming",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/gpu-types && npm run build --workspaces --if-present",
    "dev": "npm run dev --workspace=packages/demos",
    "test": "npm run test --workspaces --if-present",
    "build:demo": "cd packages/demos && vite build --config vite.config.ghpages.ts && touch ../../docs/demo/.nojekyll"
  },
  "engines": {
    "node": ">=20"
  }
}
