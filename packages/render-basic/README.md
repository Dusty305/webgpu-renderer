# @webgpu-streaming/render-basic

Базовый проход рендеринга для `webgpu-streaming`. Предоставляет `configureRenderer()` для настройки освещения и параметров рендеринга по умолчанию, используемых встроенным конвейером рендеринга.

